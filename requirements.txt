# EcoCompute energy-methodology MLCube — top-level runtime deps (exact pins).
#
# Pinned to the versions verified in the built CUDA image. bitsandbytes must
# match the torch/CUDA build; its NF4/INT8 kernels change between releases, so
# do NOT loosen these without re-verifying quantization behaviour.
#
# For a fully reproducible build (all transitive deps pinned) install
# requirements.lock.txt instead — that is what the Dockerfile uses.
torch==2.13.0
transformers==4.57.6
bitsandbytes==0.43.3
accelerate==1.14.0
nvidia-ml-py==13.610.43
pyyaml==6.0.3
sentencepiece==0.2.2
